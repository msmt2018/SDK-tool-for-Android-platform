git init
git branch -M main
git remote add origin https://github.com/msmt2018/SDK-tool-for-Android-platform.git
git remote set-url origin https://github.com/msmt2018/SDK-tool-for-Android-platform.git
git add .
#git config --global core.compression 0  #压缩级别
#git config --global http.postBuffer 2073741824 #缓冲区大小

git commit -m "Add China region high-speed download image JSON，To cope with download errors and slow speed issues"

git push -f origin main


#git pull origin ZeroStudio
#git pull origin ZeroStudio --rebase
